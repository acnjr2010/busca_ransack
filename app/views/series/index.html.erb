<p id="notice"><%= notice %></p>

<h1>Buscar Séries</h1>

<%= search_form_for @series do |f| %>
  <%= f.label :name_serie_cont, "NOME:" %>
  <%= f.search_field :name_serie_cont %>
  <%= f.submit %>
<% end %>

<h1>Series</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Seasons</th>
      <th>Year</th>
      <th>Kind</th>
      <th>Subtitle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @series.each do |series| %>
      <tr>
        <td><%= series.name %></td>
        <td><%= series.seasons %></td>
        <td><%= series.year %></td>
        <td><%= series.kind %></td>
        <td><%= series.subtitle %></td>
        <td><%= link_to 'Show', series %></td>
        <td><%= link_to 'Edit', edit_series_path(series) %></td>
        <td><%= link_to 'Destroy', series, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Series', new_series_path %>
